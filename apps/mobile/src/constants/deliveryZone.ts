/** Display-only mirror of the backend's delivery-zone constant (apps/api/src/modules/orders/deliveryZone.ts) — the API is the actual source of truth/enforcement. */
export const SUPPORTED_CITY = "Patna";
