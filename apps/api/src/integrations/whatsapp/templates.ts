/**
 * Placeholder template names pending Meta Business verification + template
 * approval (real lead time, tracked separately from the rest of the build —
 * see project spec §6). Update these to the actual approved template names
 * once they exist; nothing else in the codebase needs to change.
 */
export const WHATSAPP_TEMPLATES = {
  NEW_ORDER_ALERT: "tbc_new_order_alert",
  PRODUCT_RECOMMENDATION: "tbc_product_recommendation",
} as const;
